ROUND 2 of 5 — design the hard parts. Context: Specs/PROPOSAL-2026-09-12-AI-VOICE-AGENT-LISTINGS.md (v1) and your own Round 1 report Specs/codex-rounds/round1-astra.md. The owner has decided: (a) early hang-up = customer pays the FULL slot; (b) platform failure (provider error/capacity/our bug) = 100% refund, no pro-rata; (c) creator share (80% after 20% fee) goes to the admin's wallet UID; (d) memory kept until the customer taps "Forget me"; (e) admin = one person, hdavy2002@gmail.com, resolved to a UID at provisioning and stored in an env var, not a public flag; (f) no OPENAI_API_KEY in prod yet — lane must fail closed until it exists; (g) tables prefixed agent_live_*; private storage = DIGITAL bucket.

Produce CONCRETE, implementable designs (TypeScript-level detail, SQL, event sequences) for:

1. SEAT AUTHORITY — one Durable Object `AgentSeatAuthorityDO` (single global instance, SQLite storage). Give: the SQLite schema (reservations with agent_id, buyer, start, end, state: provisional|confirmed|active|released|expired, lease token/fence, expires_at), the exact admission algorithm using boundary sweep for peak occupancy against BOTH caps (per-agent cap passed in, platform cap from config), the RPC methods (reserveProvisional, confirm, release, acquireRuntimeLease, heartbeatLease, releaseLease, nextFreeStart(agent, minutes, from), freeStartsForDay(agent, minutes, dayStartMs, dayEndMs, gridMs)), alarm-driven expiry of provisional reservations (5 min) and stale leases, and how D1 agent_live_bookings stays a projection. State exactly what happens on: two simultaneous 'talk now' requests for the last seat; a reconnect within grace; an early permanent end (seat released; whole-interval fit for new bookings).

2. MONEY STATE MACHINE — for a booking: quote → checkout_operation row (idempotency key + request hash) → wallet hold(opId 'agentlive:hold:<bookingId>') → seat confirm → booking 'booked'; compensation if confirm fails (refund with deterministic opId, retried by a durable job row, never assumed). Settlement decision table (one immutable row per order): outcomes completed_full (release full amount to admin UID, 20% fee), refunded_platform_failure (refund 100%), cancelled_by_customer_early (>=10 min before start: refund 100%), no_show (customer never connected: full charge — service availability evidence = seat lease was available and provider healthy; define the evidence fields). Note ledger.release has no opId and computes rel:/fee: ids itself; refund/hold do accept opId; escrow visibility is eventually consistent (409 possible) → design the retry job (D1 table agent_live_money_jobs + cron every 5 min + alarm). Who runs settlement: DO finalize enqueues; cron sweeps.

3. LIVE PROTOCOL CONTRACT — write the exact message sequence between browser ⇄ AgentLiveRoom DO ⇄ wss://api.openai.com/v1/live/sessions. Decide the audio format (recommend ONE rate for input+output; state the AudioPipeline.ts changes: mic worklet target rate, playback context rate). Give the session.start JSON we send (model gpt-live-1, audio format, voice, instructions = frontend prompt, delegation {type:'responses', responses:{model:'gpt-6-astra', instructions, tools:[functions only: search_knowledge, remember_fact, get_customer_time, describe_shared_image]}}). State how we (i) refresh the time block (session.update with new delegation instructions — confirm that is the supported mechanism), (ii) inject 'customer shared a photo: <analysis>' (session.commentary.append vs session.thinking.append + response.create — recommend), (iii) handle function calls (which server event carries them, and how response.item.create + response.create answers them), (iv) build both transcripts (session.input_transcript.delta / session.output_transcript.delta), (v) close gracefully (session.close → session.closed with usage), (vi) handle error events and 429/capacity. Give the browser⇄DO JSON envelope (binary frames for PCM, JSON for control: ready, caption, timer, image_ack, ended{reason}). Decide relay vs WebRTC for v1 and justify in 5 lines.

4. RAG — recommend the concrete path for PDFs: OpenAI Files + Vector Store with our DO calling POST /v1/vector_stores/{id}/search inside the search_knowledge function (managed parsing, no chunking code) vs Cloudflare Vectorize + Workers AI embeddings + our own PDF text extraction. Pick one, give the ingest pipeline (admin upload → R2 DIGITAL → queue/waitUntil → OpenAI file → vector store file → poll status → agent_live_kb_files.status), limits, and deletion.

5. MEMORY + INJECTION SAFETY — the memory record shape (summary ≤1500 chars, facts ≤ 40 × 160 chars, open_threads ≤ 10), the prompt block format with explicit 'untrusted data' fencing, the end-of-session summariser call (Responses API, JSON schema output), CAS via updated_at/version, erasure_generation for Forget-me racing a late summariser, the remember_fact tool constraints. Where does timezone come from and how is 'It is 21:40 Saturday for the customer' rendered.

6. IMAGE SIDE CHANNEL — endpoint, validation (bytes sniff, ≤8MB, ≤ 6 images/session, decode dims cap), R2 DIGITAL key layout, vision call (Responses API with input_image, model gpt-6-astra or a cheaper vision model — recommend), persona-specific image_instructions, latency budget, how the analysis reaches the DO (DO fetch handler POST /image-analysis with session generation check), and the safety rule text for palmistry (entertainment, no medical claims).

7. ADMIN + JOIN + FLAGS — the admin gate (env AGENT_ADMIN_UIDS resolved from the email once; how to resolve email→uid safely using Clerk backend API with CLERK_SECRET_KEY), the /j/:token extension (ics.ts token kind 'agent', join_link.ts branch that reads agent_live_bookings instead of bookings/commercial_entitlements, destination /talk/<bookingId>, JoinLink.tsx union + whitelist), and the final flag table with types matching config.ts's boolean/numericKeys/stringKeys validation.

Write everything as a single markdown document Specs-ready (this becomes spec v2 sections). Cite files you read. Do not modify files.
