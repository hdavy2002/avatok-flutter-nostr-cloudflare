**NO-GO.** Six findings are fixed; seventeen remain partial. `cd worker && npx tsc --noEmit` **passed, exit 0, no diagnostics**. No files modified.

A. Verification against the current code. Severities below describe what remains.

| ID | Verdict | Verification / remaining change |
|---|---|---|
| F1 | **FIXED** | Cancellation now requires `booked` + `held`, preventing the original unfunded cancellation/release. Recovery races remain under F3. |
| F2 | **PARTIAL — P1** | Decision/job/projection fencing is fixed, but [agent_live_room.ts:1594](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1594) discards the returned winning decision. It persists and reports its own possibly losing charge/refund outcome. Adopt the winner throughout finalization. |
| F3 | **PARTIAL — P0** | [money.ts:323](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:323) checks for a decision separately from the unconditional booking update at line 337. A refund between those operations can still become `booked/held`. Put the decision exclusion and expected phase into the UPDATE itself; fence subsequent recovery transitions too. |
| F4 | **PARTIAL — P1** | Immediate startup scheduling is fixed. [money.ts:344](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:344) still ignores scheduling acknowledgement; reconciliation at line 546 excludes already-started windows. Require successful scheduling and recover current windows, including instant bookings. |
| F5 | **PARTIAL — P0** | Explicit failure refunds are fixed. [agent_live_room.ts:590](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:590) still treats an allocated provider socket as readiness. The socket exists before `session.started`; attachment followed by customer-end during that handshake can charge without delivered service. Gate billable attachment and `ready` on confirmed readiness. |
| F6 | **FIXED** | Probe completion deadline, final heartbeat freshness/gap checks, and nested failure parsing are present. |
| F7 | **PARTIAL — P0** | Acquisition now rechecks occupancy. However, [admin.ts:507](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/admin.ts:507) changes capacity without advancing its revision; the authority accepts same-revision overwrites. Stale requests can restore a higher cap. Also, `applyPolicy` has no production caller. Add monotonic capacity revisions and propagate policy changes to the authority. |
| F8 | **PARTIAL — P0** | Cleanup is centralized, but [agent_live_room.ts:850](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:850) can finish its provider fetch after concurrent cancellation and install/start the socket anyway. Recheck terminal state after startup awaits, close late sockets, and make failed seat cleanup retryable. |
| F9 | **PARTIAL — P0** | Shared close handling and explicit close-event tracking are fixed. [agent_live_room.ts:1418](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1418) still equates a missing in-memory socket with “never opened.” After eviction, persisted session/lease evidence can exist without that socket. Preserve uncertainty until positive closure evidence, consistent with the [provider closing contract](https://developers.openai.com/api/docs/guides/voice-websockets?api=live). |
| F10 | **FIXED** | Wrap-up supplies `delegation_id: null`. |
| F11 | **PARTIAL — P1** | Response-scoped lifecycle accounting replaces the debounce. But [agent_live_room.ts:1219](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1219) marks a duplicate **still-running** call done immediately. That can empty the response’s pending set before its result exists. Join the existing execution and finish accounting only when its result is available. |
| F12 | **PARTIAL — P0** | Transcript clearing and captured generation are added. However, [agent_live_room.ts:1712](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1712) writes R2 before checking generation. An in-flight snapshot can recreate erased content after deletion. Old tool buffers/executions also survive. Fence persistence and tools with their captured generation, including cleanup of racing writes. |
| F13 | **PARTIAL — P0** | Room callback/tool guards are fixed. [media.ts:303](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/media.ts:303) compares against the image row’s unchanged generation, not the current memory generation. Forget-me does not invalidate that D1 row, so late analyses still persist; image PUTs can race deletion. Fence against authoritative generation and invalidate/clean up old image data. |
| F14 | **PARTIAL — P0** | Live startup, tools, refresh and enqueue paths check memory flags. [memory.ts:425](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/memory.ts:425) does not: queued summaries still read transcripts, call the provider and write memory after disabling it. Recheck effective flags at execution and commit. |
| F15 | **FIXED** | Join-link redemption checks immutable decisions and rejects accepted cancellations/refunds, including late cancellation. |
| F16 | **PARTIAL — P1** | Startup inserts the session row. But [agent_live_room.ts:446](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:446) permits image reservations based on socket existence before that insertion; insertion errors are swallowed at line 1696. Require confirmed readiness and successful session persistence before reservation. |
| F17 | **PARTIAL — P2** | Atomic ownership prevents duplicate vision spending. [media.ts:260](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/media.ts:260) only checks duplicate content against an available R2 object; missing objects/read errors accept different bytes under the same ID. Store the content hash atomically with ownership. |
| F18 | **PARTIAL — P1** | Expired new holds are blocked. [ledger.ts:218](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/ledger.ts:218) still translates any non-200 operation lookup into `found:false`. Recovery can treat an already-debited hold as absent and abandon its refund. Distinguish lookup failure from authoritative absence. |
| F19 | **PARTIAL — P1** | Expiry replay and raced hash comparison are fixed. [checkout.ts:191](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/checkout.ts:191) still applies the lane gate before existing-operation lookup. Disabling the lane makes successful retries return 503. Resolve authenticated replays before new-checkout gates. |
| F20 | **FIXED** | CAS conflict schedules regeneration using fresh memory instead of reapplying stale summary text. |
| F21 | **FIXED** | Enqueue establishes the generation-fenced memory row before capturing its version. |
| F22 | **PARTIAL — P1** | Cutoff and completion fencing are added. But [money.ts:219](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:219) claims the job, then `runEraseJob` tries to claim it again at [memory.ts:758](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/memory.ts:758): deferred erasure repeatedly does nothing. Share one claim/token. Also fence deletion by object generation/time; booking creation time does not protect later content in previously booked sessions. |
| F23 | **PARTIAL — P1** | Test bookings receive a terminal status update, but [agent_live_room.ts:1606](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1606) swallows its failure and then marks finalization committed. A transient failure permanently recreates the overdue-row problem. Let the existing retry mechanism handle that failure. |

B. New client/integration findings.

| ID | Severity / location | Concrete defect and fix |
|---|---|---|
| B1 | **P1 — [AgentBookingBox.tsx:138](/Users/davy/Documents/websites/avaTOK-2-Flutter/web/src/islands/agent-live/AgentBookingBox.tsx:138)** | Every retry obtains a new quote while retaining the old idempotency key. After a successful booking response is lost, retry conflicts; line 173 then generates a new key, allowing the next retry to purchase a second booking. Preserve the original quote/key until that operation’s outcome is resolved. |
| B2 | **P1 — [AgentTalkRoom.tsx:315](/Users/davy/Documents/websites/avaTOK-2-Flutter/web/src/islands/agent-live/AgentTalkRoom.tsx:315)** | Worker booking responses contain `amount`; the web adapter drops it and the receipt sums nonexistent `receiptLines`. Paid/refunded sessions display **₹0**. Carry the authoritative amount through `AgentBooking` and render it directly. |
| B3 | **P1 — [AgentTalkRoom.tsx:105](/Users/davy/Documents/websites/avaTOK-2-Flutter/web/src/islands/agent-live/AgentTalkRoom.tsx:105)** | Scheduled purchases immediately redirect here, but prejoin rejects calls earlier than the two-minute join window. The UI shows a generic failure instead of a booking confirmation/lobby. “My bookings” uses the commercial schedule; agent list/cancel helpers have no callers. Add a persistent agent-booking view with scheduled-time handling, cancellation and receipts independent of prejoin. |
| B4 | **P1 — [AgentTalkRoom.tsx:241](/Users/davy/Documents/websites/avaTOK-2-Flutter/web/src/islands/agent-live/AgentTalkRoom.tsx:241)** | The permission hook holds one microphone stream while `AudioPipeline` acquires another. Pipeline failure is swallowed and the billable connection proceeds. The permission stream remains active after ending; exhausted reconnect paths also leave pipeline capture running. Own one stream, await successful audio initialization, and stop all capture on terminal/refused/lost states. |
| B5 | **P1 — [admin.ts:506](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/admin.ts:506)** | The `slot_minutes → listings.attrs` integration fix applies on creation only. Admin edits update the agent row but not listing attributes, leaving the Flutter detail screen advertising stale durations. Mirror changes into `attrs` in the same batch, preserving other attributes. |
| B6 | **P2 — [AgentTalkRoom.tsx:281](/Users/davy/Documents/websites/avaTOK-2-Flutter/web/src/islands/agent-live/AgentTalkRoom.tsx:281)** | An image-ready acknowledgement arriving before the upload HTTP response cannot match the UI’s still-null image ID. The response then unconditionally sets `analyzing`, ignoring returned status; the image can remain spinning permanently. Merge response status and buffer early acknowledgements. |

The uploader now sends `client_upload_id`; room-generation validation and browser adoption are present; validated `hello.timezone` is persisted. The duration mirror remains incomplete as B5 describes.

**Dart symbol check:** no missing introduced repo-defined symbols found. This includes the remote-config getter, listing attributes, `AD` members, toast export and analytics methods. Package references were checked against dependencies and existing usage. No Flutter compilation or analysis was run.

C. Deployment checklist.

| Check | Result |
|---|---|
| Production and staging DO bindings | **OK** — both authority and room bindings present. |
| Production and staging `v24` | **OK** — both SQLite DO classes registered in each environment. |
| Production variables | **OK for existing values** — no existing production vars changed/removed. The latest file adds the explicit `AGENT_ADMIN_UIDS` UID; staging remains empty. Live identity/secret validity was not verified. |
| SQLite migration | **OK** — all 15 statements are `CREATE … IF NOT EXISTS`; applied twice to in-memory SQLite successfully, integrity check passed, financial CHECKs are consistent with the implemented full-charge/full-refund outcomes. |
| Config key classification | **OK** — numeric and string agent flags are registered in the corresponding key sets. |
| Index exports, route mount, cron | **OK structurally** — exports/mount/sweep invocation present. Recovery correctness remains blocked by findings above. |
| Worker TypeScript | **PASS** — `npx tsc --noEmit`, exit 0. |
| Diff whitespace | **PASS** — `git diff --check`. |

Desktop Commander shell and Graphify calls were rejected by automatic approval review because approvals are disabled; sandboxed read-only tools were used for verification.

**NO-GO for production deploy — must fix F2–F5, F7–F9, F11–F14, F16, F18–F19, F22–F23, and B1–B5 first. F17 and B6 remain nonblocking P2 defects.**