ROUND 5 of 5 — FINAL VERIFICATION. Working tree (uncommitted) now contains fixes for your Round 4 findings F1–F23 (Specs/codex-rounds/round4-astra.md) plus four integration fixes (client_upload_id sent by the web uploader; slot_minutes mirrored into listings.attrs; X-Room-Gen validated by the room; browser adopts the room's connectionGeneration; hello.timezone persisted to buyer_tz).

Part A — verify each F1…F23 against the CURRENT code (git diff / new files under worker/src/lib/agent_live, worker/src/routes/agent_live, worker/src/do/agent_seat_authority.ts, worker/src/do/agent_live_room.ts, worker/src/ledger.ts, worker/src/do/wallet.ts, worker/src/routes/{join_link,checkout}.ts). For each: FIXED / PARTIAL (what remains, file:line) / NOT FIXED. Be strict but concrete; do not re-open items on style grounds.

Part B — review the web + app side for the same class of real defects (money, security, broken flow, contract mismatch with the worker): web/src/lib/agentLive.ts, web/src/islands/agent-live/*, web/src/islands/admin-agents/*, web/src/pages/talk/[booking].astro, web/src/pages/admin/agents/*, web/src/components/ListingDetailsComp.astro (agent branch), web/src/islands/agent/AudioPipeline.ts, web/src/islands/join/JoinLink.tsx, web/src/lib/analytics.ts, web/src/lib/apiClient.ts; app/lib/core/listing_groups.dart, app/lib/core/remote_config.dart, app/lib/features/explore/native_listing_detail_v2.dart (Dart: check every symbol exists in the repo — there is no Flutter toolchain, a typo costs an hour of CI).

Part C — deployment checklist sanity: worker/wrangler.toml (both bindings + v24 in prod AND staging; no accidental prod var changes), worker/migrations/2026-09-12-agent-live.sql (valid SQLite, CREATE IF NOT EXISTS only, CHECKs consistent), config.ts flags (stringKeys/numericKeys), index.ts exports/mount/cron.

Output: (A) table F-id → verdict; (B) new findings with severity/file:line/fix; (C) checklist ok/not; final line "GO / NO-GO for production deploy" with the exact list of anything that must change first. Do NOT modify files. Also run `cd worker && npx tsc --noEmit` and report.
