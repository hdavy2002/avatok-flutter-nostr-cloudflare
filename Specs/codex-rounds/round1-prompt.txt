You are auditing a proposal for a new product lane in this repo (Cloudflare Worker in worker/, Astro+React web in web/, Flutter app in app/). ROUND 1 of 5.

Read Specs/PROPOSAL-2026-09-12-AI-VOICE-AGENT-LISTINGS.md and Specs/AUDIT-2026-09-12-AI-AGENT-REUSE-MAP.md, then read CLAUDE.md sections: PRODUCT PIVOT, PAID-SESSION RULEBOOK, TOKENS, flags rules.

Task A — do your OWN independent audit of the code the proposal intends to reuse: worker/src/do/agent_voice_room.ts, worker/src/routes/avavoice.ts (composePrompt, mintToken, activeCalls, settleSession), worker/src/routes/join_link.ts + web/src/islands/join/JoinLink.tsx, worker/src/routes/admin_money.ts requireAdmin, worker/src/ledger.ts hold/release/refund, worker/src/routes/config.ts DEFAULTS/putConfig/numericKeys pattern, worker/src/lib/listing_section.ts, worker/src/routes/listings.ts createListing (KINDS, sectionFor, billing_unit), web/src/islands/agent/AudioPipeline.ts, web/src/pages/l/[id].astro, web/src/lib/apiClient.ts. Verify every claim the proposal makes about these files (cite file:line). List every claim that is WRONG or imprecise.

Task B — critique the proposal's design: the concurrency seat engine (D1 batch atomicity — is insert/count/delete in one D1 batch actually atomic and race-free? propose the correct primitive if not, e.g. a per-agent Durable Object as the seat authority), the DO relay architecture vs WebRTC, the memory design (privacy, prompt-injection from transcripts, size), the image side channel, the money flow (hold/release/refund opIds, who receives the creator share), the join-link extension, admin gating by email vs uid, the flags list, and anything missing (rate limiting, abuse, minors/adults_only, refunds on our failures, reconnect semantics, cron sweeps for stuck sessions, R2 privacy, GDPR/Forget-me).

Output a markdown report with: (1) Wrong/imprecise claims (file:line evidence), (2) Design risks ranked by severity with a concrete fix each, (3) Missing pieces, (4) Your recommended changes to the spec as a numbered list. Be specific and terse. Do NOT modify any files.
