ROUND 4 of 5 — CODE REVIEW of the implemented worker lane. The build spec is Specs/SPEC-2026-09-12-AGENT-LIVE-1-BUILD.md (incl. §11 amendments M1–M13 that adopted your Round 3 blockers). Your R2/R3 reports are in Specs/codex-rounds/. The code is now in the working tree (uncommitted): run `git status --short` and `git diff` to see modified files; new files are under worker/src/lib/agent_live/, worker/src/routes/agent_live/, worker/src/do/agent_seat_authority.ts, worker/src/do/agent_live_room.ts, worker/migrations/2026-09-12-agent-live.sql, plus edits to worker/src/ledger.ts, worker/src/do/wallet.ts, worker/src/routes/{admin_money,join_link,listings,config,wallet}.ts, worker/src/cal/ics.ts, worker/src/index.ts, worker/wrangler.toml, worker/src/types.ts.

Review for REAL DEFECTS only (things that would lose money, double-charge, leak data, break capacity, crash at runtime, or violate the OpenAI Live wire contract). For each finding give: file:line, what happens (concrete scenario), severity (P0 = must fix before prod, P1 = fix soon, P2 = nit), and the minimal fix (a short code snippet where useful). Prioritise:
1. money.ts decideAndEnqueue / runMoneyJob / runAgentLiveSweeps and ledger.ts releaseExact + wallet.ts retention: idempotency, CAS/lock correctness, double refund/release, orphan holds, job kinds summarise/erase dispatch.
2. checkout.ts agentQuote/agentBook: gate before hold, quote signature/expiry, idempotency replay, seat reserve→hold→confirm→schedule ordering and every failure branch's compensation; 402 path; cancel proxy.
3. agent_seat_authority.ts: admission algorithm (both caps, boundary sweep, provider_uncertain union), instant rebase in confirm, fences on heartbeat/releaseRuntime, alarm expiry, SQL injection/typing, transactionSync misuse (any await inside).
4. agent_live_room.ts + openai_live.ts + talk.ts: session.start shape, base64 audio relay, function-call path (response.event → output_item.done → function_call → response.item.create → response.create), transcripts, session.update coalescing, session.close/closed, error/429 handling, evidence for no-show, finalize idempotency + D1 replay, reconnect grace, room token verification in talk.ts (HMAC compare, expiry), WS header trust boundary (can a client forge X-Room-* headers?), image-reserve/analysis generation checks.
5. join_link.ts/ics.ts agent branch: fail-closed secret, hash compare, cancelled/refunded rejection; admin_money agl_ guard; listings.ts bypass guards; config.ts validation of the 11 flags (types/bounds; stringKeys); wrangler.toml bindings + v24 migration in BOTH prod and staging blocks; index.ts DO exports + dispatcher mount + cron hook.
6. media.ts/vision.ts/memory.ts/prompt.ts: DIGITAL bucket used everywhere (never BLOBS), prompt fencing/escaping, CAS on summariser, forget-me generation checks, image sniffing/limits, no secrets in prompts, no transcripts in telemetry.

Also run `cd worker && npx tsc --noEmit` and report the result. Output a terse markdown report: findings table sorted by severity, then a one-line verdict "prod-ready after fixing: <P0 list>". Do NOT modify files.
