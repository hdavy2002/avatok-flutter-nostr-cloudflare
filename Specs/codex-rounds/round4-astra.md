`cd worker && npx tsc --noEmit` **passed (exit 0, no diagnostics)**. No files modified.

| ID | Severity | File:line | Concrete failure | Minimal fix |
|---|---|---|---|---|
| F1 | **P0** | [checkout.ts:338](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/checkout.ts:338) | Cancellation accepts a **pending, unfunded** booking. For an instant booking, the room chooses full charge and `releaseExact` credits the beneficiary even if the subsequent hold returns 402. Checkout can also continue after cancellation. | Serialize cancellation against checkout; require a verified successful hold before any release decision. CAS every checkout transition against terminal decisions. |
| F2 | **P0** | [money.ts:106](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:106) | A losing decision attempt still inserts its job and overwrites booking status **before** checking the winning hash. Even an identical replay resets `released/refunded` to pending while the completed job immediately returns. | Make job insertion and projection updates conditional on the persisted decision hash. Preserve completed money states; return the actual winning decision to the room. |
| F3 | **P0** | [money.ts:375](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:375) | Refunded confirmation failures retain `confirm_pending`. Sweeps repeatedly recover them; if abort failed and the reservation remains confirmed, recovery rewrites the refunded booking as `booked/held`. Terminal rows can also repeatedly consume the recovery limit. | Set `checkout_phase='aborted'` atomically with terminal decisions. Claim recovery with CAS and exclude existing decisions; guard `finalizeBookedBooking` similarly. |
| F4 | **P0** | [agent_live_room.ts:683](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:683) | Instant confirmation makes `starts_at <= now`. Scheduling therefore arms **`ends_at−60s`**, not startup. A 20-minute “Talk now” session stays silent for approximately 19 minutes; admin tests suffer the same defect. | When the provider has not started, arm `Math.max(now + 1, starts_at)` before considering wrap-up alarms. Require scheduling acknowledgement; reconcile already-started booking windows too. |
| F5 | **P0** | [agent_live_room.ts:1263](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1263) | Heartbeat rejection calls finalization with `platform_error`; unexpected socket closure uses `provider_error` without necessarily setting `evidence.providerError`. Both can become **full charge** because a browser attached. Lobby attachment also counts as service delivery. | Refund explicit platform/provider/capacity failures before the attachment branch. Record billable attachment only after successful provider readiness. |
| F6 | **P0** | [agent_live_room.ts:1271](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1271) | A readiness probe returning after the required ten-second deadline still qualifies for no-show charging. After room eviction, persisted evidence can also qualify despite no heartbeat during the final minutes. Nested backend failures are ignored by the parser. | Check probe completion against `starts_at+10s`, validate the final heartbeat gap, and persist nested delegation failures. Missing evidence must refund. |
| F7 | **P0** | [agent_seat_authority.ts:605](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_seat_authority.ts:605) | With cap 1, adjacent bookings A/B are confirmed. A becomes provider-uncertain past its end; B nevertheless acquires a lease because acquisition never rechecks capacity. Separately, `applyPolicy` stores limits that admission never reads. | Recheck both caps against occupied/uncertain intervals during lease acquisition. Use authority-owned current policy and reject stale cap revisions. |
| F8 | **P0** | [agent_live_room.ts:1283](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1283) | Early cancellation and startup failure call `commitTerminal` directly, bypassing seat/provider cleanup. Cancelled future slots remain reserved; a `session.started` timeout can leave an opened upstream socket alive after refund. | Route every terminal path through idempotent cleanup: release/abort unused reservations; close opened providers and retain uncertainty until verified closed. |
| F9 | **P0** | [agent_live_room.ts:1214](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1214) | Concurrent finalizers overwrite the shared close resolver. One timed-out finalizer clears it, allowing another to infer `providerClosed=true`. A transport close also counts as confirmed session closure without `session.closed`. | Use one shared finalization promise and an explicit `sessionClosedReceived` flag. Only positive terminal evidence clears uncertainty. [Closing contract](https://developers.openai.com/api/docs/guides/voice-websockets?api=live). |
| F10 | **P0** | [openai_live.ts:139](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/openai_live.ts:139) | The wrap-up `session.instructions.append` omits required nullable `delegation_id`. At the one-minute warning, the invalid command can produce an error that terminates/refunds an otherwise successful call. | Add `delegation_id: null`. [Live reference](https://developers.openai.com/api/reference/resources/live/primary-websocket). |
| F11 | **P0** | [agent_live_room.ts:1123](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1123) | A 200ms debounce sends `response.create` after the first fast tool completes, even when another function item from the same response arrives later. The global pending set also mixes responses. | Collect calls by response/delegation; wait for the response lifecycle boundary and all corresponding results before continuing once. [Delegation contract](https://developers.openai.com/api/docs/guides/live-delegation). |
| F12 | **P0** | [agent_live_room.ts:365](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:365) | Forget me adopts the new erasure generation but retains the entire old transcript. Finalization writes that transcript back to R2 and queues it using the **new generation**, allowing deleted facts to reappear. | Keep an immutable session capture generation; invalidate old transcript/tool buffers and summarization on erasure. Check generation before persistence, with cleanup for writes racing erasure. |
| F13 | **P0** | [agent_live_room.ts:431](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:431) | A late image result after Forget me matches its old image record, passes validation and is spoken. `describe_shared_image` also serves old analyses; media’s D1 updates have no erasure predicate. | Compare callbacks and tool reads against **current room generations** and terminal state; invalidate old images on forget and CAS D1 writes against current erasure generation. |
| F14 | **P0** | [agent_live_room.ts:771](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:771) | Setting `agentMemoryEnabled=false` only changes the prejoin response. The room still loads customer memory into prompts, executes memory writes and queues summaries. | Enforce the effective memory flag throughout provider startup, refresh, tools and summarization; clear existing context when disabled. |
| F15 | **P0** | [join_link.ts:86](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/join_link.ts:86) | Late cancellation produces `status='completed'`, which this branch accepts. The cancelled booking’s emailed link can still mint a **full-account Clerk sign-in ticket**. | Check the immutable decision and reject accepted cancellations/refunds, including `customer_cancel_late`, before minting a ticket. |
| F16 | **P1** | [media.ts:189](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/media.ts:189) | Every live image upload returns `session_not_found`: the room only inserts `agent_live_sessions` during finalization. Failed attempts still consume room image reservations. | Create/upsert the session row at startup, including its generations, before permitting image reservations. |
| F17 | **P1** | [media.ts:227](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/media.ts:227) | Once session-row creation is fixed, replaying an upload ID reruns vision: reservation returns the same image ID, so `existing.id !== imageId` is false. Retries can repeatedly spend provider money and overwrite the same image. | Use insert/CAS ownership to select exactly one uploader/analyser. Return the stored result for duplicate IDs; reject different content under the same ID. |
| F18 | **P1** | [money.ts:359](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/money.ts:359) | Crash after persisting `hold_pending` but before calling WalletDO: recovery initiates a **new debit after quote expiry**, potentially after the reservation expired or the lane was disabled. | Distinguish successful prior operations from unattempted holds; never create an expired hold. Treat lookup failure as unknown, not absent. |
| F19 | **P1** | [checkout.ts:205](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/routes/agent_live/checkout.ts:205) | Retrying a successful checkout after two minutes returns `quote_expired` instead of its booking. The insert-race branch also returns another request’s booking without comparing request hashes. | Resolve existing buyer/key operations before expiry/new-checkout gates; compare hashes on both normal and raced replay paths. |
| F20 | **P1** | [memory.ts:546](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/memory.ts:546) | On CAS failure, the code reloads memory but reapplies the **same stale summary**, overwriting a concurrent session’s newer summary. | Regenerate against the fresh version, or leave the job retryable; do not reuse stale summary text with a newer CAS version. |
| F21 | **P1** | [memory.ts:538](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/memory.ts:538) | A first session without `remember_fact` has no memory row: `loadMemory` creates nothing and `touchSeen` is never called. Both summarizer UPDATE attempts affect zero rows, losing the session’s memory. | Create the generation-fenced memory row before capturing the summary version. |
| F22 | **P1** | [memory.ts:663](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/lib/agent_live/memory.ts:663) | A delayed erase job selects **all bookings at execution time**, including conversations created after Forget me, and deletes their new objects. Job updates also lack lock-token fencing, while inline erasure bypasses claiming. | Freeze an erasure cutoff/generation; delete only older objects. Run erasure through the shared claim path and fence completion updates. |
| F23 | **P1** | [agent_live_room.ts:1294](/Users/davy/Documents/websites/avaTOK-2-Flutter/worker/src/do/agent_live_room.ts:1294) | Admin tests skip the only booking-status update, remaining `booked` forever. Repeated overdue test rows can fill the sweep’s 20-row limit and delay recovery of paid bookings. | Terminalize test booking status without creating financial decisions/jobs; exclude completed tests from sweeps. |

**prod-ready after fixing: F1–F15.**