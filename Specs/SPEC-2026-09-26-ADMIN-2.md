# SPEC — Admin 2 (fresh admin dashboard) + delete old dashboards — [ADMIN2-*] — 2026-09-26

Owner request (2026-09-26): "delete old customer dashboard and delete old admin dashboard. now we have
new customer dashboard and lets build new admin dashboard". Saa Thum is a DIRECT service: Saa Thum
itself creates and runs every puja/havan (no outside creators). Environment: PRODUCTION.

## Work location — IMPORTANT
All agents work ONLY in the git worktree `/Users/davy/Documents/websites/avaTOK-2-Flutter/.wt-admin2`
(branch `admin2`, based on origin/main). In device_bash it is `$HOME/mnt/avaTOK-2-Flutter/.wt-admin2`.
It has its own node_modules (installed on macOS). NEVER touch the main checkout (other sessions have
uncommitted work there). Commit with plain `git -C <worktree> add <paths> && git commit -m "[ADMIN2-<AREA>] …"`
(explicit paths only; end the message with the two attribution lines below). Never push, deploy,
set flags or write to remote D1 — the coordinator does that.
  Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_0176BnsFBybLNHNRsNF1bgh5
device_bash (Linux VM): read/edit/tsc only, NEVER npm install. Desktop Commander (macOS): builds, vitest,
git; each call must return in <55s → run long commands in background with a /tmp log and poll.

## Look
Same design system as Dashboard 2 (customer): `web/src/components/ui/*` (shadcn), `web/src/styles/saathum-tokens.css`
(.dash2 scope), Comfortaa + Nunito, `font-dash`/`font-dashbody`, `bg-card`, `border-border` (gold), etc.
Reuse `web/src/layouts/Dashboard2.astro` patterns. POSH, responsive (admin is used on the phone too).
No bare var(--x) in Tailwind arbitrary classes (always a fallback). No raw hex in components.
Brand is displayed "Saa Thum".

## Routes (web) — new admin lives at /admin
| Menu | Route |
|---|---|
| Overview | /admin |
| Events | /admin/events (list) · /admin/events/new · /admin/events/[id] (edit) |
| Bookings | /admin/bookings (all bookings; filter by event) |
| Payments | /admin/payments |
| Refunds | /admin/refunds |
| Customers | /admin/customers · /admin/customers/[uid] |
| Prices | /admin/prices |
| Back to site / Logout | / · /sign-out |
Layout: `web/src/layouts/Admin2.astro` (props: title, heading?, subtitle?, active) + nav island
`web/src/islands/admin2/AdminNav.tsx`. Screens: `web/src/islands/admin2/*.tsx`.
Admin guard: signed out → /sign-in?redirect_url=…; signed in but not admin (worker returns 403 on an
admin probe, e.g. GET /api/admin/overview or a new GET /api/admin/whoami) → "You don't have admin access".

## Screens
- Overview: KPI tiles (today's & next 7 days' events, bookings today/7d, revenue today/7d/30d in ₹,
  open refund requests, pending payments), "Next events" list with LIVE badge, quick actions.
- Events: table/cards of Saa Thum's listings (upcoming / live / past / draft / cancelled), search,
  create/edit form (title, category from taxonomy, deity, description, cover image upload, date+time IST,
  duration, price ₹, capacity optional, YouTube unlisted link, publish/unpublish, cancel). Reuse the
  existing worker listing routes (POST /api/listings, PUT /api/admin/listings/:id, publish/approve paths,
  cover upload, listing_blockers rules, admin YouTube route) — read Specs + memory notes in the spec header
  of those files; admin-created listings must end up published without a creator review loop.
  Per event: attendee count and link to Bookings filtered by that event.
- Bookings: every seat (paid/free/pending), customer name/email/phone(masked), event, status, amount,
  UTR, booked at; filters; CSV export.
- Payments: all payment lines across customers (same sources as /api/me/payments but admin-wide:
  orders + commercial_policy_snapshots + hdfc_sms_payment_intents), filters, totals, CSV export.
- Refunds: move the existing AdminRefunds island into the new shell (same API).
- Customers: search by name/email/phone-last-4; profile view with their bookings, payments, refunds.
- Prices: move the existing AdminPricing island (PRICING-1) into the new shell (same API).

## Deletions (owner decision — delete, git history keeps them)
- Old customer dashboard: `web/src/pages/archive/dashboard-v1/**`, v1-only islands in
  `web/src/islands/dashboard/**` and anything only they use; remove the v1 fallback so /dashboard ALWAYS
  renders Dashboard 2 (flag `dashboard2Enabled` no longer consulted; leave the key declared in config.ts).
  Old v1 URLs keep redirecting to the nearest Dashboard 2 screen.
- Old admin: `web/src/pages/admin/{listings,reviews}.astro`, `web/src/pages/admin/agents/**`, the old
  `web/src/islands/admin/**` and `web/src/islands/admin-agents/**` — EXCEPT reuse (move) AdminRefunds,
  AdminPricing, YouTubePanel logic into admin2. `web/src/layouts/Dashboard.astro` is deleted once nothing
  imports it. Old admin URLs redirect: /admin/listings→/admin/events, /admin/pricing→/admin/prices,
  /admin/reviews→/admin, /admin/agents/*→/admin.
- Worker API routes are NOT deleted (the app and other tools may still call them).

## New worker routes (admin-only via requireAdmin from routes/admin_money.ts)
GET /api/admin/whoami · GET /api/admin/v2/overview · GET /api/admin/v2/bookings · GET /api/admin/v2/payments
· GET /api/admin/v2/customers?q · GET /api/admin/v2/customers/:uid · any events helpers needed
(e.g. GET /api/admin/v2/events). Register them through the dash2 dispatcher pattern in
worker/src/routes/me_dashboard.ts / index.ts (one small hunk). Money = paise ints. Tests in vitest.

## Shell usage (agent A, [ADMIN2-SHELL])
Page (`export const prerender = false;`):
```astro
import Admin2 from '../../layouts/Admin2.astro';
import AdminEvents from '../../islands/admin2/AdminEvents.tsx';
<Admin2 title="Events" active="events" subtitle="Every puja and havan Saa Thum runs.">
  <Fragment slot="actions"><a href="/admin/events/new" class="…">New event</a></Fragment>  {/* optional */}
  <AdminEvents client:load />
</Admin2>
```
- `active`: `overview|events|bookings|payments|refunds|customers|prices`. `heading?` overrides the h1.
- The layout already emits `admin2_view {screen}`, mounts the sonner Toaster (`toast()` from
  `components/ui/sonner`) and hides the slot + actions until the admin probe says OK.
- Islands: NO ClerkIsland. Call the worker with `adminApi<T>(path, {method, body, query})` from
  `islands/admin2/adminApi.ts` (waits for the guard, attaches the Clerk token). Also there:
  `adminBlob()` + `saveBlob()` for CSV, `formatPaise`, `istDate/istDateTime/istDayToMs`, `errMessage`, `isAbort`.
- Worker: add your handler file, then register ONE line in your marked section of
  `worker/src/routes/admin2.ts` (`// --- events (agent B) ---`, `// --- bookings/payments/customers (agent C) ---`).
  Every `/api/admin/v2/*` path already reaches `admin2Route` from index.ts. Use `adminGuard(req, env)` from
  admin2.ts (403 → `{error:"admin_only"}`).

## Checks before reporting done
worker `npx tsc --noEmit` clean; vitest for new tests; web build
`PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuYXZhdG9rLmFpJA npm run build` then from web/:
`node scripts/check-homepage.mjs && node scripts/check-help.mjs && node scripts/check-image-urls.mjs && node scripts/check-performance.mjs`;
`python3 tool/check_design_guard.py --check all`; manifest entry per ADMIN2-* id in tool/ship_manifest.json.
Telemetry via web/src/lib/analytics.ts: admin2_view{screen}, admin2_event_saved{action}, admin2_export{kind}.
