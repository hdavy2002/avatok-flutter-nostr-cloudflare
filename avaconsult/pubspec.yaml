name: avaconsult
description: AvaConsult — paid 1:20 group calling (Cloudflare RealtimeKit SFU).
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  permission_handler: ^11.3.1
  google_fonts: ^6.2.1
  realtimekit_ui: ^0.4.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
