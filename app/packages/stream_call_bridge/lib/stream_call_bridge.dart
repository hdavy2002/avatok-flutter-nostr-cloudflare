/// Native registration package for AvaTOK's optional Stream call pilot.
///
/// Call code talks to the stable MethodChannel/EventChannel contract in the
/// main app; this library deliberately exposes no provider SDK types to Dart.
library stream_call_bridge;
