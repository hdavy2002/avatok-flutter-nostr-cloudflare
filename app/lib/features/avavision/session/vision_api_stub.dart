// PHASE-3 LOCAL STUB — RETIRED at Phase Z (2026-06-13).
//
// This file previously held a local mirror of `VisionAgent` / `AvaVisionApi` /
// `VisionSessionTicket` so Phase 3 could compile before Phase 2 merged. During
// the Phase Z glue the canonical surface was unified into
// `app/lib/core/avavision_api.dart` (which now also exposes `VisionSessionTicket`,
// `AvaVisionApi.sessionToken`, `sessionHeartbeat`, and `snapshotRaw`). The session
// files (`vision_engine.dart`, `vision_session_screen.dart`) now import core
// directly. This file is intentionally empty and is NOT committed to git.
//
// (The FUSE-mounted workspace blocks file unlink, so it is emptied rather than
// removed; it is excluded from the Phase Z commit.)
