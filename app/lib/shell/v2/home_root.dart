// DEPRECATED (2026-07-12 nav rebrand): the Home dashboard root was retired —
// AvaTOK (AvaTalk) is now the sole landing app, and the app-switcher footer moved
// to `app_switcher_bar.dart` / `ShellV2` itself so it stays visible across every
// app instead of living on a dedicated Home screen. This file is intentionally
// left as an empty, unreferenced stub (nothing imports it) rather than deleted,
// since file deletion isn't available in this environment. Do not re-add a
// `RootId.home` — it no longer exists in `shell_v2.dart`.
