// DEPRECATED (2026-07-12 nav rebrand): superseded by `app_switcher_bar.dart`
// (`AppSwitcherBar`), which is rendered once at the `ShellV2` level so the same
// icons stay in the same place across every app. This file is intentionally left
// as an empty, unreferenced stub (nothing imports it) rather than deleted, since
// file deletion isn't available in this environment.
