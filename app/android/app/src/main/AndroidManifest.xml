<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="com.sec.android.provider.badge.permission.READ" />
    <uses-permission android:name="com.sec.android.provider.badge.permission.WRITE" />
    <uses-permission android:name="com.htc.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="com.htc.launcher.permission.UPDATE_SHORTCUT" />
    <uses-permission android:name="com.sonyericsson.home.permission.BROADCAST_BADGE" />
    <uses-permission android:name="com.sonymobile.home.permission.PROVIDER_INSERT_BADGE" />
    <uses-permission android:name="com.anddoes.launcher.permission.UPDATE_COUNT" />
    <uses-permission android:name="com.majeur.launcher.permission.UPDATE_BADGE" />
    <uses-permission android:name="com.huawei.android.launcher.permission.CHANGE_BADGE" />
    <uses-permission android:name="com.huawei.android.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="com.huawei.android.launcher.permission.WRITE_SETTINGS" />
    <uses-permission android:name="me.everything.badger.permission.BADGE_COUNT_READ" />
    <uses-permission android:name="me.everything.badger.permission.BADGE_COUNT_WRITE" />
    <uses-permission android:name="com.oppo.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="com.oppo.launcher.permission.WRITE_SETTINGS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- [PLAY-MEDIA-PERMS-1 2026-08-05] READ_MEDIA_IMAGES / READ_MEDIA_VIDEO /
         READ_MEDIA_AUDIO are REMOVED. Google flagged them in the Play "Photo and
         video permissions" declaration and the app never actually needed them:

           • Every gallery/attachment flow goes through image_picker (1.1.x →
             image_picker_android 0.8.13) or file_picker (8.x). image_picker uses
             ActivityResultContracts.PickVisualMedia (the system Photo Picker,
             backported to old Android via the Play-services photopicker module
             the plugin's own manifest pulls in) or ACTION_GET_CONTENT; file_picker
             uses the Storage Access Framework. All of those hand back a
             already-granted content:// URI — NO runtime read permission at ANY
             API level. The only permission image_picker_android ever requests is
             CAMERA (ImagePickerDelegate.needRequestCameraPermission), which is
             declared above.
           • Nothing in the app reads media by path or queries MediaStore for
             READING. The one MediaStore use (AvaDialPlugin.kt, saving a call
             recording) INSERTS into MediaStore.Downloads, which needs no
             permission on API 29+ and falls back to WRITE_EXTERNAL_STORAGE below
             that.
           • No audio is ever picked from the device's music library, so
             READ_MEDIA_AUDIO had no caller at all.

         The matching "Photos & media" step was removed from the onboarding
         permission page in the same change (features/onboarding/onboarding_flow.dart)
         so we no longer ask for a permission we don't hold.

         tools:node="remove" is belt-and-braces: no current plugin or AAR declares
         these (verified against every hosted package's library manifest and the
         GIPHY/coil/glide/transcoder/mlkit/exifinterface AARs), but a future
         dependency bump must not silently re-add them and re-trigger the Play
         declaration. If a feature ever genuinely needs gallery-wide access,
         delete these three lines rather than adding a duplicate declaration. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- CALL-BG-B4: required on Android 14+ (API 34) to declare "camera" in the
         call service's foregroundServiceType for video calls. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- [PLAY-SCOPE-1 2026-08-05] AvaTOK is AvaTOK-to-AvaTOK calling only. It is NOT
         a dialer replacement: no default-dialer role, no call-log reading, no SMS, no
         spam call screening, no missed-call overlay. READ_CALL_LOG, ANSWER_PHONE_CALLS
         and SYSTEM_ALERT_WINDOW are therefore all REMOVED.

         The two that REMAIN are used solely by the AI receptionist's carrier
         call-forwarding setup:
           • CALL_PHONE      — dials the carrier MMI/USSD forwarding codes
                               (*61*<DID>#) via AvaDialPlugin.placeMmiCall, so the
                               user's unanswered calls divert to the receptionist DID.
                               See app/lib/features/avadial/pstn_forwarding_*.dart.
           • READ_PHONE_STATE — names the default voice SIM + reads the SIM operator
                               code so we pick the RIGHT carrier's forwarding syntax,
                               and lets AvaMissedCallReceiver notice an unanswered
                               incoming call so it can pre-register the forwarded leg
                               with the worker (PSTN "expect" ping). No call content,
                               no call log, no number history is read.
         MANAGE_OWN_CALLS is declared above for the in-network VoIP path and is NOT
         what makes an app the default dialer — the ROLE_DIALER role does that at
         runtime, and AvaTOK never requests it. -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- [ONBOARD-STREAMLINE-1 2026-07-23] The default-SMS-app layer (AVA-SMS) is
         REMOVED: AvaTOK no longer becomes or asks to become the default SMS app,
         so the Play-"restricted" SMS permissions (SEND_SMS / RECEIVE_SMS /
         READ_SMS / RECEIVE_MMS / RECEIVE_WAP_PUSH) and the SMS_DELIVER /
         WAP_PUSH_DELIVER / RESPOND_VIA_MESSAGE / SENDTO components below are all
         gone. -->

    <application
        android:label="AvaTOK"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- [ISSUE-DUPTASK] Recents duplication — corrected diagnosis.
             The old comment here claimed flutter_callkit_incoming's call activity
             uses the default package affinity, and that giving MainActivity an
             empty affinity was therefore what split them into two tasks. That was
             WRONG. The plugin declares CallkitIncomingActivity with
             launchMode="singleInstance" AND its own dedicated
             taskAffinity="com.hiennv.flutter_callkit_incoming.INCOMING_CALL_AFFINITY",
             so it is ALWAYS in its own task no matter what MainActivity does.
             Removing the empty affinity could never have merged them, which is why
             the second Recents card kept coming back. Two real fixes:
               1. Below: MainActivity is singleTask, not singleTop. The ongoing-call
                  notification relaunches it with FLAG_ACTIVITY_NEW_TASK from a
                  service context (CallForegroundService.kt); a NEW_TASK launch of a
                  singleTop activity can spawn a SECOND MainActivity task instead of
                  reusing the existing one. singleTask forces task reuse.
               2. Further down: we merge autoRemoveFromRecents onto the plugin's
                  CallkitIncomingActivity so its task disappears when the ring
                  activity finishes. The plugin's excludeFromRecents + noHistory are
                  honoured on stock Android but unreliable on the Samsung/Oppo/
                  Huawei/Xiaomi launchers we target (see badge permissions above). -->
        <!-- [ISSUE-DUPTASK 2026-08-05] `android:taskAffinity=""` REMOVED — it was
             the other half of this bug, and it is why fix (1) above did not stick.

             `singleTask` reuses an existing task by looking one up BY AFFINITY.
             An empty affinity means "this activity has no affinity", so there is
             nothing to match on: every FLAG_ACTIVITY_NEW_TASK launch from
             CallForegroundService.kt:107 (the ongoing-call notification, a
             SERVICE context, which is why NEW_TASK is mandatory there) failed to
             find the running task and started a SECOND one. Hence two AvaTOK
             cards in Recents, reliably after a call or a call-back — exactly the
             trigger reported on 2026-08-05, alongside two Play in-app-update
             surfaces because the update UI renders inside whichever task hosts it.

             The empty affinity was added on the mistaken belief (recorded, and
             corrected, in the comment above) that it would separate MainActivity
             from the CallKit ring task. It never could: the plugin declares
             `launchMode="singleInstance"` plus its OWN dedicated
             `taskAffinity="com.hiennv.flutter_callkit_incoming.INCOMING_CALL_AFFINITY"`,
             so that activity is in its own task regardless. Removing the empty
             string restores the default (package name) affinity, which is what
             `singleTask` needs to do its job — and changes nothing about the
             CallKit, IncomingCallActivity or InCallActivity tasks, all of which
             carry explicit affinities of their own. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 5 (A1): Android App Links — https://avatok.ai/j/<token>
                 opens the app directly (verified via /.well-known/assetlinks.json). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="avatok.ai" android:pathPrefix="/j/"/>
            </intent-filter>
            <!-- App Link — https://avatok.ai/add?t=<token> opens the app to the
                 "add contact" card (AvaTOK Number QR share). Same host as /j/, so
                 the existing /.well-known/assetlinks.json verifies it too. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="avatok.ai" android:pathPrefix="/add"/>
            </intent-filter>
            <!-- [MKT7] App Link — https://avatok.ai/l/<id> opens a marketplace
                 listing (QR on the listing detail page). Same host as /j/ and /add,
                 so the existing /.well-known/assetlinks.json verifies it too.
                 iOS follow-up: add "/l/*" to the hosted apple-app-site-association. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="avatok.ai" android:pathPrefix="/l/"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="avatok.ai" android:pathPrefix="/a/"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="avatok.ai" android:pathPrefix="/i/"/>
            </intent-filter>
            <!-- Custom-scheme deeplink fallback (avatok://booking/<id>, avatok://add?t=). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="avatok"/>
            </intent-filter>
        </activity>
        <!-- [PLAY-SCOPE-1 2026-08-05] The native PSTN call screens
             (ai.avatok.avadial.IncomingCallActivity / InCallActivity) are REMOVED
             along with the default-dialer story. AvaTOK does not host carrier calls;
             AvaTOK-to-AvaTOK VoIP rings through CallkitIncomingActivity below. -->
        <!-- [ISSUE-DUPTASK] Merge-override of flutter_callkit_incoming's ring
             activity. We do NOT change its launchMode or taskAffinity (singleInstance
             on a dedicated affinity is what lets it show over the lock screen), we
             only add autoRemoveFromRecents so the task it creates is torn down as
             soon as the ring activity finishes — killing the ghost card that lingers
             after a call on OEM launchers. These attributes are absent upstream, so
             the manifest merger adds them without needing tools:replace.
             tools:node="merge" keeps the plugin's own declaration intact. -->
        <activity
            android:name="com.hiennv.flutter_callkit_incoming.CallkitIncomingActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            tools:node="merge" />
        <activity
            android:name="com.hiennv.flutter_callkit_incoming.TransparentActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            tools:node="merge" />

        <!-- [AVATOK-VOIP-TC-1] Keep AvaTOK P2P VoIP calls OUT of the Android Telecom
             framework. flutter_callkit_incoming ships a self-managed
             CallkitConnectionService (BIND_TELECOM_CONNECTION_SERVICE +
             android.telecom.ConnectionService); with MANAGE_OWN_CALLS declared, every
             VoIP ring is reported to Telecom as a system call. Third-party caller-ID /
             call-screening apps (e.g. Truecaller) observe that Telecom call and paint
             their own overlay BEHIND our ring — both bound to the same call object, so
             declining tears the connection down and Truecaller dismisses in lockstep.
             AvaTOK P2P (VoIP + AI answering machine) is a SEPARATE product from AvaDial
             (PSTN/SMS); it needs only the notification + CallkitIncomingActivity
             full-screen UI, and its audio is owned by WebRTC + CallForegroundService,
             NOT the Telecom connection. Removing this service declaration leaves Telecom
             nothing to bind, so the call stays in-app and invisible to other dialers.
             MANAGE_OWN_CALLS is intentionally KEPT (full-screen-intent priority on
             Android 14+). Does NOT touch AvaDial's own telecom services (ai.avatok.avadial
             AvaInCallService / AvaCallScreeningService) — different components entirely. -->
        <service
            android:name="com.hiennv.flutter_callkit_incoming.CallkitConnectionService"
            tools:node="remove" />

        <!-- flutter_web_auth_2 callback for the avatokauth:// OAuth return
             (AvaStorage "Connect Google Drive"). Lets the in-app auth tab
             auto-close and hand control straight back to the app. Distinct from
             the "avatok" deeplink scheme above. -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="avatokauth" />
            </intent-filter>
        </activity>
        <!-- CALLFIX-20 / CALL-BG-B4: Foreground service for ongoing calls (keeps VoIP
             calls alive while backgrounded). Declares phoneCall|microphone|camera so
             BOTH audio-only and video calls are covered — Android 14+ (API 34)
             requires every type actually started via ServiceCompat.startForeground to
             be pre-declared here; audio calls simply never pass the camera bit at
             runtime. android:stopWithTask defaults to false (unset = false), i.e. the
             service is NOT killed when the app's task is removed from Recents — this
             is required for the call to survive the user swiping the app away. -->
        <service
            android:name="ai.avatok.avavoiceaudio.CallForegroundService"
            android:foregroundServiceType="phoneCall|microphone|camera"
            android:stopWithTask="false"
            android:exported="false" />
        <!-- ── AvaDial telecom layer (DARK behind the `avaDialer` flag) ──────────
             See Specs/SPIKE-2026-07-12-avadial-telecom.md. -->

        <!-- [ONBOARD-STREAMLINE-1 2026-07-23] The default-dialer in-call surface
             (AvaInCallService, BIND_INCALL_SERVICE + IN_CALL_SERVICE_UI) is REMOVED
             along with the ACTION_DIAL alias below: AvaTOK no longer becomes or asks
             to become the default dialer, so it declares no InCallService and is not
             an eligible ROLE_DIALER candidate. The spam call-screening service is a
             SEPARATE role (ROLE_CALL_SCREENING) and is kept. -->

        <!-- [PLAY-SCOPE-1 2026-08-05] Spam call-screening (AvaCallScreeningService /
             ROLE_CALL_SCREENING) is REMOVED. AvaTOK no longer screens carrier calls,
             so it declares no CallScreeningService and is not an eligible
             ROLE_CALL_SCREENING candidate. -->

        <!-- [ONBOARD-STREAMLINE-1 2026-07-23] The ACTION_DIAL / tel: activity-alias
             (DialAlias) is REMOVED — it existed only to satisfy the ROLE_DIALER
             requirement that a default dialer handle dial intents, and AvaTOK no
             longer seeks that role. -->

        <!-- [ONBOARD-STREAMLINE-1 2026-07-23] The default-SMS-app layer is REMOVED:
             the SMS_DELIVER receiver (AvaSmsReceiver), its OTP "Copy code" receiver
             (AvaOtpCopyReceiver), the WAP_PUSH_DELIVER receiver (AvaMmsReceiver), the
             RESPOND_VIA_MESSAGE service (AvaSmsSendService) and the ACTION_SENDTO
             compose alias (SmsComposeAlias) are all gone. AvaTOK is not an eligible
             ROLE_SMS candidate. -->

        <!-- [PLAY-SCOPE-1 2026-08-05] AvaCallActionReceiver (the Answer/Decline actions
             on the native PSTN incoming-call notification) is REMOVED along with the
             rest of the default-dialer surface. -->

        <!-- [AVA-RCPT-5 / PLAY-SCOPE-1] AI-receptionist PSTN "expect" ping ONLY.
             Listens for the system PHONE_STATE broadcast and, when an incoming call
             rings out unanswered (RINGING→IDLE without OFFHOOK), fires a single
             fire-and-forget POST to /api/pstn/expect-native so the worker can map the
             leg the carrier has ALREADY forwarded to the receptionist DID back to this
             owner. It reads no call log, shows no overlay/popup, and stores nothing —
             the missed-call overlay that used to live here is gone.
             exported=true because the OS is the broadcaster; READ_PHONE_STATE
             (declared above) is what authorises delivery. Fully DARK unless Dart has
             written {enabled:true} into pstn_config.json (the `pstnVoicemail` flag). -->
        <receiver
            android:name="ai.avatok.avadial.AvaMissedCallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            <provider
            android:name="com.superlist.super_native_extensions.DataProvider"
            android:authorities="${applicationId}.SuperClipboardDataProvider"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required (Android 11+) so the native speech recognizer is visible to
             speech_to_text; without it initialize() fails and STT falls back to
             cloud Whisper. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <!-- AvaDial: resolve dial intents (Android 11+ package visibility) so we can
             place PSTN calls / detect other dialers. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- AvaDial: resolve SMS send intents so we can detect other messaging apps. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="smsto"/>
        </intent>
        <!-- [AVADIAL-SETUP-1] Explicit package visibility (Android 11+) for known
             third-party caller-ID / dialer apps that draw their own overlay, so the
             setup sheet can DETECT the installed ones (getApplicationInfo) and
             deep-link straight to each one's settings. We can only detect + link —
             Android never lets one app disable or revoke another. Keep this list in
             sync with knownRivalPackages in AvaDialPlugin.kt. -->
        <package android:name="com.truecaller" />
        <package android:name="mobi.drupe.app" />
        <package android:name="com.callapp.contacts" />
        <package android:name="com.eyecon.global" />
        <package android:name="com.hiya.star" />
        <package android:name="gogolook.callgogolook2" />
    </queries>
</manifest>
