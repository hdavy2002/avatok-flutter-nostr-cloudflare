# [WEB-SEO-2 2026-09-10] www.avatok.ai -> avatok.ai is NOT here. It was tried as
#   https://www.avatok.ai/* https://avatok.ai/:splat 301
# and Cloudflare Pages ignored the host-form rule (www kept answering 200), while
# the Astro adapter parsed it as a path and emitted a junk "/https:/www.avatok.ai/*"
# entry in _routes.json. The redirect lives as a zone Redirect Rule in the
# Cloudflare dashboard instead (Rules -> Redirect Rules, "[WEB-SEO-2] www.avatok.ai
# -> avatok.ai (301)"), which is the layer that actually sees the hostname.

# [WEB-SEO-2] Retired URLs Google still holds from the pre-pivot site (Search
# Console "Crawled - currently not indexed", 10 URLs, last crawled Jun/Jul
# 2026). Left alone they either 404 or — worse — fall into the /[username]
# mockup catch-all and answer 200 with a demo profile. A 301 to the page that
# replaced them hands over whatever link equity they had and clears the report.
/videos                       /marketplace   301
/video-sessions/*             /marketplace   301
/live-events/*                /marketplace   301
/creator-resources            /ideas         301
/creator-resources/*          /ideas         301
/how-it-works                 /about         301
/coming-soon                  /              301

# [WEB-INDIA-HOME-1] One canonical creator homepage.
/india                        /              301
/india/                       /              301

# [WEB-GATEWAY-C] Creator-idea guides removed as unsafe (companionship/listening/
# loneliness sessions, or styling/draping sessions) — see Specs/WEBGW-C-REPORT.md.
/blog/creator-ideas/dil-ki-baat            /ideas   301
/blog/creator-ideas/nri-homesick-adda      /ideas   301
/blog/creator-ideas/saree-draping-help     /ideas   301
/blog/creator-ideas/style-your-own-wardrobe /ideas  301
/blog/creator-ideas/saree-draping-circle   /ideas   301
/blog/creator-ideas/shaam-ka-adda          /ideas   301

# [WEB-GATEWAY-INT] The close-friends-studio redirect is NOT here on purpose —
# this file's exclude entries in dist/_routes.json are already at Cloudflare's
# 100-rule cap, so a new literal entry here gets silently dropped at build
# time and the URL falls through to the Worker (200, not 301). See
# src/pages/blog/global-creator-ideas/close-friends-studio.astro instead —
# a literal sibling page that redirects via Astro.redirect, which needs no
# _routes.json slot. Specs/WEBGW-INT-REPORT.md has the full story.
