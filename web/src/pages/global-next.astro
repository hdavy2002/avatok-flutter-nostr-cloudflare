---
// [WEB-GATEWAY-INT 2026-09-18] Preview page, not a product page — meta
// description used banned marketing language ("biggest fans", "private 1:1
// video meetups") a payment-gateway reviewer could stumble onto.
// Permanent-redirect so any indexed/shared link still resolves.
export const prerender = false;
return Astro.redirect('/', 301);
---
