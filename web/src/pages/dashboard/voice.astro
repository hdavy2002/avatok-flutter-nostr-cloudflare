---
// /dashboard/voice — old v1 dashboard URL. [ADMIN2-DELETE 2026-09-26] The v1 dashboard is
// deleted (git history keeps it) and /dashboard always renders Dashboard 2, so this
// page only redirects to the nearest Dashboard 2 screen (/dashboard).
export const prerender = false;
return Astro.redirect(`/dashboard${Astro.url.search}`, 301);
---
