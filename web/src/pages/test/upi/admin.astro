---
// [TAKEDOWN-HDFC-DARK-1 2026-09-20] Admin console for the internal ₹1 HDFC UPI SMS
// smoke harness (worker/src/routes/hdfc_sms_*.ts). The Worker routes are gated
// behind the hdfcSmsRailEnabled flag (410 while off, see worker/src/index.ts and
// routes/config.ts) — not the commercial rail, and must not be reachable during a
// payment gateway review. This page stays a hard 404 regardless of that flag;
// restoring the UI needs its own deliberate revert of this file.
export const prerender = false;
return new Response('Not found', { status: 404 });
---
