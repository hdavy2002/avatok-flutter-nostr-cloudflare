---
// [TAKEDOWN-HDFC-SMOKE-1 2026-09-20] Admin console for the internal ₹1 HDFC UPI SMS
// smoke harness (worker/src/routes/hdfc_sms_*.ts), which is now unregistered from the
// Worker router — not the commercial rail, and must not be reachable during a payment
// gateway review. Kept as a 404 rather than deleted so the harness can be restored by
// reverting this file and the Worker route registrations together.
export const prerender = false;
return new Response('Not found', { status: 404 });
---
