---
// /dashboard — the signed-in Overview (mirrors the app's AvaVerse home).
// Renders inside the Dashboard shell (sidebar). Content is an island because it
// is per-user; the shell guards auth.
export const prerender = false;

import Dashboard from '../layouts/Dashboard.astro';
import Overview from '../islands/dashboard/Overview.tsx';
---

<Dashboard title="Dashboard · avatok.ai" active="overview">
  <Overview client:load />
</Dashboard>
