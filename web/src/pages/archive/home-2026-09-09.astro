---
// [WEB-GATEWAY-INT 2026-09-18] Archived homepage draft, not a product page —
// Hinglish/companionship-flavored copy a payment-gateway reviewer could
// stumble onto (already noindex,nofollow, but still publicly readable).
// Permanent-redirect so any indexed/shared link still resolves. The archived
// source HTML stays on disk under src/landing/archive/ for history.
export const prerender = false;
return Astro.redirect('/', 301);
---
