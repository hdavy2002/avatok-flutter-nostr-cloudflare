---
export const prerender = true;
import Content from '../layouts/Content.astro';
---

<Content
  kicker="Talk to a human"
  title="Contact us"
  description="Get in touch with Saathum — support, press, partnerships, or to report something that isn't right."
  image="/og/contact.png"
  lead="Questions, feedback, press, partnerships, or a problem to report? Your message goes straight to the Saathum support team."
  width="wide"
  tone="pink"
>
  <p>
    <ui-copy style="display:contents" data-i18n="web-contact.3561417b16521b57">You can always email us directly at</ui-copy> <a href="mailto:support@avatok.ai">support@avatok.ai</a><ui-copy style="display:contents" data-i18n="web-contact.5ebe030573ca42a3">, or
    use the form below. For copyright issues see our</ui-copy> <a data-i18n="web-contact.0606b89431ebdb4b" href="/dmca">DMCA / Copyright</a> <ui-copy style="display:contents" data-i18n="web-contact.26b6e19408dd9c56">policy; for
    anything about your data, see our</ui-copy> <a data-i18n="web-contact.506ff394621596dd" href="/privacy">Privacy Policy</a><ui-copy style="display:contents" data-i18n="web-contact.908bc4dab31f73e1">. To file a formal
    complaint, use</ui-copy> <a data-i18n="web-contact.b5d03028fbc1d7ac" href="/grievance">Grievance Redressal</a>.
  </p>

  <h2 data-i18n="web-contact.8739a7eb5fa17cb1" id="status">Who we are and where to find us</h2>


  <form id="contact-form" class="cform" novalidate>
    <label class="honeypot" aria-hidden="true">
      <span data-i18n="web-contact.78dd534863d4fb0a">Company website</span>
      <input type="text" name="company" tabindex="-1" autocomplete="off" />
    </label>
    <div class="row">
      <label>
        <span data-i18n="web-contact.2c6b2e253c01fb2e">Your name</span>
        <input type="text" name="name" autocomplete="name" required maxlength="120" />
      </label>
      <label>
        <span data-i18n="web-contact.969ccbd3cf6300ec">Email</span>
        <input type="email" name="email" autocomplete="email" required maxlength="200" />
      </label>
    </div>
    <label>
      <span data-i18n="web-contact.80d7546c2da066f8">What can we help with?</span>
      <select name="category">
        <option data-i18n="web-contact.f2d5cbe11cddc437" value="Support">Product support</option>
        <option data-i18n="web-contact.045cad4755b7ffcc" value="Feedback">Feedback or an idea</option>
        <option data-i18n="web-contact.9ffa741ded2441d8" value="Safety">Safety or abuse report</option>
        <option data-i18n="web-contact.6e7dbc6b206c441e" value="Privacy">Privacy request</option>
        <option data-i18n="web-contact.57e09dc50abdcdfc" value="Press">Press or media</option>
        <option data-i18n="web-contact.b61dd8f40159a9b8" value="Partnership">Partnership</option>
        <option data-i18n="web-contact.cc72843144841a6f" value="Other">Something else</option>
      </select>
    </label>
    <label>
      <span data-i18n="web-contact.68971283841aecdf">Subject</span>
      <input data-india-i18n-attr="placeholder:web-contact.416de4ff9b44bc18" type="text" name="subject" maxlength="160" placeholder="What’s this about?" />
    </label>
    <label>
      <span data-i18n="web-contact.2f77668a9dfbf8d5">Message</span>
      <textarea data-india-i18n-attr="placeholder:web-contact.7b56e0d0b308abe4" name="message" rows="6" required maxlength="5000" placeholder="How can we help?"></textarea>
    </label>
    <button data-i18n="web-contact.93a26b1eaff99b3a" type="submit" class="cbtn">Send message</button>
    <p class="cstatus" id="contact-status" role="status" aria-live="polite"></p>
    <p class="delivery-note"><ui-copy style="display:contents" data-i18n="web-contact.5ff3490d40ebce43">Every form submission is delivered through Brevo to</ui-copy> <strong>support@avatok.ai</strong>.</p>
  </form>
</Content>

<style>
  .cform { width: min(100%, 820px); margin: 36px auto 0; padding: clamp(22px, 4vw, 42px); display: flex; flex-direction: column; gap: 20px; border: 1px solid #181816; border-radius: 24px; background: #fffdf7; box-shadow: 12px 12px 0 #4f5cff; }
  .cform .row { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
  .cform label { display: flex; flex-direction: column; gap: 8px; font-weight: 800; color: #181816; font-size: 17px; }
  .cform input, .cform textarea, .cform select {
    width: 100%; font-family: 'DM Sans', sans-serif; font-size: 17px; font-weight: 600; color: #181816;
    background: #fff; border: 1px solid #181816; border-radius: 15px;
    padding: 14px 16px; outline: none; box-shadow: 3px 3px 0 #181816; transition: box-shadow .12s ease, transform .12s ease;
  }
  .cform input:focus, .cform textarea:focus, .cform select:focus { box-shadow: 5px 5px 0 #4f5cff; transform: translate(-1px,-1px); }
  .cform textarea { resize: vertical; }
  .cform .honeypot { position: absolute; left: -10000px; width: 1px; height: 1px; overflow: hidden; }
  .cbtn {
    align-self: center; font-family: 'DM Sans', sans-serif; font-weight: 800; font-size: 17px;
    color: #181816; background: #ffd95e; border: 1px solid #181816; border-radius: 999px;
    padding: 14px 28px; cursor: pointer; box-shadow: 4px 4px 0 #181816;
    transition: transform .12s ease, box-shadow .12s ease;
  }
  .cbtn:hover:not(:disabled) { transform: translate(-2px,-2px); box-shadow: 6px 6px 0 #181816; }
  .cbtn:disabled { opacity: .6; cursor: default; }
  .cstatus { font-weight: 800; margin: 0; min-height: 1.2em; text-align: center; }
  .cstatus.ok { color: #176b43; }
  .cstatus.err { color: #b42525; }
  .delivery-note { margin: 0; font-size: 14px; color: rgba(24,24,22,.65); text-align: center; }
  @media (max-width: 560px) { .cform { box-shadow: 7px 7px 0 #4f5cff; } .cform .row { grid-template-columns: 1fr; } }
</style>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement | null;
  const statusEl = document.getElementById('contact-status');
  if (form && statusEl) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const btn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
      const data = Object.fromEntries(new FormData(form).entries());
      if (!data.name || !data.email || !data.message) {
        statusEl.className = 'cstatus err';
        statusEl.textContent = 'Please fill in your name, email, and message.';
        return;
      }
      btn.disabled = true;
      const prev = btn.textContent;
      btn.textContent = 'Sending…';
      statusEl.className = 'cstatus';
      statusEl.textContent = '';
      try {
        const res = await fetch('/api/contact', {
          method: 'POST',
          headers: { 'content-type': 'application/json' },
          body: JSON.stringify(data),
        });
        const out = await res.json().catch(() => ({}));
        if (res.ok && out.ok) {
          form.reset();
          statusEl.className = 'cstatus ok';
          statusEl.textContent = 'Thanks! Your message is on its way — we’ll be in touch.';
        } else {
          statusEl.className = 'cstatus err';
          statusEl.textContent = out.error || 'Could not send your message. Please email support@avatok.ai.';
        }
      } catch {
        statusEl.className = 'cstatus err';
        statusEl.textContent = 'Network error. Please email support@avatok.ai.';
      } finally {
        btn.disabled = false;
        btn.textContent = prev;
      }
    });
  }
</script>
