---
// /admin/reviews — old admin URL. [ADMIN2-DELETE 2026-09-26] The reviews queue is retired.
// The old admin is deleted (git history keeps it); this page only redirects.
export const prerender = false;
return Astro.redirect(`/admin${Astro.url.search}`, 301);
---
