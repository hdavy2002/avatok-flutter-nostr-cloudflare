---
// [ADMIN2-USERS 2026-09-26] Customers became Users: /admin/customers/<uid> opens that user's panel.
export const prerender = false;
const uid = String(Astro.params.uid ?? '').slice(0, 200);
return Astro.redirect(uid ? `/admin/users?user=${encodeURIComponent(uid)}` : '/admin/users', 301);
---
