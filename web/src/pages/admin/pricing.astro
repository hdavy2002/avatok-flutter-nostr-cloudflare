---
// /admin/pricing — old admin URL. [ADMIN2-DELETE 2026-09-26] Pricing lives at /admin/prices in Admin 2.
// The old admin is deleted (git history keeps it); this page only redirects.
export const prerender = false;
return Astro.redirect(`/admin/prices${Astro.url.search}`, 301);
---
