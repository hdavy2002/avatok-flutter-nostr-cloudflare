---
// /admin/events/[id] — [ADMIN2-EVENTS 2026-09-26] edit / publish / unpublish / cancel one
// event. Specs/SPEC-2026-09-26-ADMIN-2.md. The island loads the event client-side (admin API).
export const prerender = false;
import Admin2 from '../../../layouts/Admin2.astro';
import EventForm from '../../../islands/admin2/EventForm.tsx';

const id = String(Astro.params.id ?? '').slice(0, 200);
---

<Admin2 title="Edit event" active="events">
  <EventForm client:load eventId={id} />
</Admin2>
