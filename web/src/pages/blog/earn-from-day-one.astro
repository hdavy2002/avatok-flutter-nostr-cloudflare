---
// Old creator-economy post — superseded by the AvaTok launch positioning.
// Permanent-redirect so shared links keep working.
export const prerender = false;
return Astro.redirect('/blog/your-private-number', 301);
---
