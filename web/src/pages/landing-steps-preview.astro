---
// [WEB-GATEWAY-INT 2026-09-18] Preview page, not a product page — was
// Hinglish and companionship-flavored copy a payment-gateway reviewer could
// stumble onto. Permanent-redirect so any indexed/shared link still resolves.
export const prerender = false;
return Astro.redirect('/', 301);
---
