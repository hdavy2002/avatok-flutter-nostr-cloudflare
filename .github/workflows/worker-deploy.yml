name: Deploy Cloudflare Worker

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment target
        required: true
        default: staging
        type: choice
        options: [staging, prod]
      apply_media_jobs_neutral:
        description: Rename the historical media job table and queue id column to neutral names
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Guard branch and target
        run: |
          if [ "${{ inputs.environment }}" = "prod" ] && [ "$GITHUB_REF_NAME" != "main" ]; then
            echo "::error::Production Worker deployments must use main."
            exit 1
          fi
          if [ "${{ inputs.environment }}" = "staging" ] && [ "$GITHUB_REF_NAME" != "staging" ]; then
            echo "::error::Staging Worker deployments must use staging."
            exit 1
          fi

      - name: Install Worker dependencies
        working-directory: worker
        run: npm ci

      - name: Validate listing authority changes
        working-directory: worker
        run: |
          npm run typecheck
          npm test -- listing_review_binding.test.ts listing_lifecycle_authority.test.ts listing_billing.test.ts listing_fee_route_contract.test.ts commercial_notifications_contract.test.ts

      - name: Select target
        run: printf '%s\n' '${{ inputs.environment }}' > .avatok-target

      - name: Apply listing authority columns
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ALLOW_PROD: ${{ inputs.environment == 'prod' && '1' || '0' }}
        run: |
          python3 scripts/d1_apply_alters.py worker/migrations/2026-09-04-listing-reviewed-version.sql --binding DB_META
          python3 scripts/d1_apply_alters.py worker/migrations/2026-09-04-listing-authority-version.sql --binding DB_META

      - name: Apply listing lifecycle projections
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ALLOW_PROD: ${{ inputs.environment == 'prod' && '1' || '0' }}
        run: scripts/cf.sh worker d1 execute DB_META --remote --file=worker/migrations/2026-09-04-listing-lifecycle-projections.sql

      - name: Apply neutral media job migration
        if: ${{ inputs.apply_media_jobs_neutral }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ALLOW_PROD: ${{ inputs.environment == 'prod' && '1' || '0' }}
        run: scripts/cf.sh worker d1 execute DB_MEDIA --remote --file=worker/migrations/2026-08-19-media-jobs-neutral.sql

      - name: Deploy Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ALLOW_PROD: ${{ inputs.environment == 'prod' && '1' || '0' }}
        run: scripts/cf.sh worker deploy
