# Stripe staging QA — 2026-06-11

Ran against `api-staging.avatok.ai`. Test user: `user_3Ey4FEyXZ0bhxagp6dYM90X6cIj`
(`qa-stripe+…@avatok.ai`, created on the live Clerk instance via Backend API +
native ticket sign-in).

## Stripe config (verified via test key `acct_1SZCqHJgoaTrScq1`, US)
- Test mode, `charges_enabled: true`, Identity **enabled** in test.
- Webhook endpoints (after cleanup) — all `enabled`, correct events:
  - `https://api.avatok.ai/webhooks/stripe` + `/webhooks/stripe-identity`
  - `https://api-staging.avatok.ai/webhooks/stripe` + `/webhooks/stripe-identity`
- **Deleted** the stale pre-pivot endpoint `https://avatok.ai/api/stripe/webhook`.

## C5 — Identity (KYC) — ✅ PASS (end-to-end)
1. `POST /api/id/session {provider:'stripe'}` → 200, real test session `vs_…`.
2. `GET /api/id/status` → `pending`.
3. Signed `identity.verification_session.verified` → `/webhooks/stripe-identity`
   → `200 {received:true, status:verified}`.
4. `GET /api/id/status` → **`verified`**, `kyc.status=verified`. ✓

## C3 — Money pipe — ⚠️ PARTIAL (core proven; ledger audit pending)
1. `POST /api/wallet/topup` → 200, Stripe Checkout `cs_test_…`, `topup_id` recorded.
2. Balance before: **0**.
3. Signed `checkout.session.completed` → `/webhooks/stripe`
   → `200 {received:true, credited:500}`.
4. Balance after: **500** ✓ (WalletDO authoritative).
5. `topup_records`: **status=paid, amount_coins=500** ✓.
6. `wallet_transactions` / `wallet_ledger`: **0 rows** ✗ — the double-entry audit
   row never appeared (waited 20s+).

**Why:** the WalletDO emits the ledger event to the `wallet-transactions-staging`
queue (`Q_WALLET`), consumed by a **separate** worker (`avatok-consumers`,
`consumers/src/wallet.ts`) that writes `wallet_transactions` + `wallet_ledger`.
On staging that consumer is not processing the queue, so the audit trail isn't
written. The money movement itself (top-up → webhook → balance credit) is fully
proven; only the async audit ledger is missing.

## Staging DB migration gaps fixed along the way (via D1 REST API)
Staging DBs were under-migrated. Applied (idempotent):
- `avatok-meta-staging`: `meta.sql` (was missing `account_status` → every
  authed request 500'd; also `profiles`, `clerk_nostr_link`, `verification_requests`).
- `avatok-wallet-staging`: `wallet.sql`, `wallet_ledger.sql`, `wallet_phase7.sql`,
  `payout.sql`, then `cfnative_c.sql` (npub→uid re-key — `topup_records` etc.
  were missing the `uid` column).

## Open follow-ups
- **Deploy / wire `avatok-consumers-staging`** to consume `wallet-transactions-staging`
  (and check its other staging queue consumers) — needed for the C3 ledger-row
  acceptance and statement CSV.
- Re-run C3's ledger assertion once the consumer is live.
- Audit staging for other unmigrated tables before device QA (live_streams was
  also missing on meta; calendar/listings looked present).
- QA artifact cleanup: the live-Clerk test user above (verified, 500 staging coins).
